import {Divider, Flex, Image, SimpleGrid, Stack, Text} from '@chakra-ui/react'

interface WebVersionsProps {
  designId: string
  versionsById: string[]
}
export function WebVersions({designId, versionsById}: WebVersionsProps) {
  return <Text>TODO</Text>
}
