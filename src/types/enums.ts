export enum DesignType {
  Mobile = 'Mobile',
  Web = 'Web',
  Logo = 'Logo',
  Illustration = 'Illustration',
  Other = 'Other',
}

export enum DesignStep {
  Create,
  Upload,
  Preview,
  Share,
}

export enum VoteStyle {
  Choose = 'choose',
  FiveStar = 'five-star',
}
